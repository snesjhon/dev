---
import PortfolioLayout from '../../layouts/PortfolioLayout.astro';
import { workItems } from '../../content/data/work';

const item = workItems.find(w => w.company === 'conDati')!;
---

<PortfolioLayout item={item} />
